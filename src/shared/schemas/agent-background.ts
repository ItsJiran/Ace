/**
 * Define interchangeable schema for the agent thread state, this is the schema that will be used to 
 * interchange data between the frontend and the backend, and also the schema that we stored in the
 * database for the agent thread state.
 */

